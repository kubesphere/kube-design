import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import Notify from "components/Notify";
import Button from "components/Button";

<Meta title="Components/Notify" component={Notify} />

# Notify

Notify

## Props
<Props of={Notify} />


## Examples

With `MDX` we can define a story for `Notify` right in the middle of our
markdown documentation.

<Preview>
  <Story name="basic notify">
    <div>
      <Button onClick={() => Notify.info('Kube Design')}>Info</Button>
      <Button onClick={() => Notify.success('Kube Design')}>Success</Button>
      <Button onClick={() => Notify.warning('Kube Design')}>Warning</Button>
      <Button onClick={() => Notify.error('Kube Design')}>Error</Button>
    </div>
  </Story>
</Preview>
